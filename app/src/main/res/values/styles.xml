<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="RatingBar"
        parent="android:style/Widget.Material.RatingBar.Small">
        <item name="colorControlNormal">@color/primary_light</item>
        <item name="colorControlActivated">@color/primary_dark</item>
        <item name="android:maxHeight">48dp</item>
        <item name="android:maxWidth">48dp</item>
    </style>

    <style name="ToolbarDestacado" parent="@style/ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="colorControlNormal">@color/textColorWhite</item>
        <item name="android:textColorPrimary">@color/textColorWhite</item>
    </style>

    <style name="boton_rojo" parent="Widget.AppCompat.Button.Colored">
        <item name="colorButtonNormal">#c46363</item>
        <item name="android:textColorPrimary">#fff</item>
    </style>

    <style name="boton_azul" parent="Widget.AppCompat.Button.Colored">
        <item name="colorButtonNormal">#638bc4</item>
        <item name="android:textColorPrimary">#fff</item>
    </style>

    <style name="titulo_ficha" parent="TextAppearance.AppCompat.Title">
        <item name="android:shadowDx">3</item>
        <item name="android:shadowDy">3</item>
        <item name="android:shadowColor">#2c2b2b</item>
        <item name="android:shadowRadius">1</item>
        <item name="android:textSize">26sp</item>
    </style>

</resources>
